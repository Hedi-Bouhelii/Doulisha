{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Doulisha internal library (TypeScript source consumed directly)",
  "extends": "./base.json",
  "compilerOptions": {
    "types": ["node"]
  }
}
